{
  "entities": {
    "Message": {
      "title": "Contact Message",
      "description": "An inquiry message submitted through the contact form with optional Google verified credentials.",
      "type": "object",
      "properties": {
        "sender_uid": { "type": "string", "maxLength": 128 },
        "sender_email": { "type": "string", "maxLength": 200, "format": "email" },
        "sender_name": { "type": "string", "maxLength": 100 },
        "sender_photo": { "type": "string", "maxLength": 500 },
        "first_name": { "type": "string", "maxLength": 100 },
        "last_name": { "type": "string", "maxLength": 100 },
        "user_email": { "type": "string", "maxLength": 200, "format": "email" },
        "message": { "type": "string", "maxLength": 2000 },
        "createdAt": { "type": "timestamp", "description": "The server timestamp when the message was created" }
      },
      "required": ["first_name", "user_email", "message", "createdAt"]
    },
    "VisitorStats": {
      "title": "Visitor Statistics",
      "description": "Aggregate tracking metrics for total unique portfolio visitors.",
      "type": "object",
      "properties": {
        "totalViews": { "type": "number", "description": "Total unique visitor count" },
        "updatedAt": { "type": "timestamp", "description": "Server timestamp of the latest recorded unique view" }
      },
      "required": ["totalViews", "updatedAt"]
    },
    "UniqueVisitor": {
      "title": "Unique Visitor Record",
      "description": "Individual record of a unique visitor session based on anonymous client identity.",
      "type": "object",
      "properties": {
        "visitorId": { "type": "string", "maxLength": 128 },
        "firstVisitedAt": { "type": "timestamp", "description": "Timestamp when this visitor first visited" }
      },
      "required": ["visitorId", "firstVisitedAt"]
    }
  },
  "firestore": {
    "/messages/{messageId}": {
      "schema": { "$ref": "#/entities/Message" },
      "description": "Stores contact messages and inquiries."
    },
    "/stats/visitors": {
      "schema": { "$ref": "#/entities/VisitorStats" },
      "description": "Global visitor metrics and unique view counters."
    },
    "/visitors/{visitorId}": {
      "schema": { "$ref": "#/entities/UniqueVisitor" },
      "description": "Registry of unique client visitor identifiers."
    }
  }
}
